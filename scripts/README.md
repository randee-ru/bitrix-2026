Каталог зарезервирован под вспомогательные скрипты.

Клонирование **bitrix-tools/env-docker** выполняется целью **`make bootstrap`** в корневом `Makefile` (без отдельного shell-файла — так проще избежать проблем с кодировкой и невидимыми символами на macOS).
